package Model.Game;

/**
 * Questa interfaccia contiene il metodo da implementare da ogni enum per confrontare le proprie 
 * costanti.
 * @author Davide Morelli
 *
 */
public interface compareAction {

	 int compareAction(Enum<?> a1);
}
